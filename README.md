# Waiting
Waiting to get in a group.


This repository is in order to push my project work on github as the classes lessons wants us to.
But should it be the group repository, this one for now is only mine cause I do not have a group yet.
So just doing some work and when times comes I'll be able to do it properly as wanted.

Firstly there is the Python branches where my file with the code made from the seven exercices are.
The seventh one required us to combine all code in one, so I pushed it a lil further and made it all into a minigame that you can simply access by dowloading the file and double clicking it.

As for the rest it's the second lesson that was on API and how to use them.
It takes one python file using code to make a link with the required API's and getting the wanted ressources.
After that making the link and lauching the html pages where the API's are used using javascript and for one API, the pokeapi mainly using python trough Jinja making some structure.
Therefore there are also many different webpages used to create my sites and have my different API's on differents html files using only one python file.
And as it is needed cause you get leave all that with a work that is just bland, a little css file making a similar display of the pages and for the things unique to them there is integrated style in the html files. 
